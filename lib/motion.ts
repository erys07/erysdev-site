/**
 * Curva de easing padrão do site — arranca rápido e chega suave.
 * Tipada como tupla porque o framer-motion não aceita number[].
 */
export const ease: [number, number, number, number] = [0.22, 1, 0.36, 1]
